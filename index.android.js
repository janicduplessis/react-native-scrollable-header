/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 */

import {AppRegistry} from 'react-native';

import ScrollableHeader from './src/ScrollableHeader';

AppRegistry.registerComponent('scrollableheader', () => ScrollableHeader);
